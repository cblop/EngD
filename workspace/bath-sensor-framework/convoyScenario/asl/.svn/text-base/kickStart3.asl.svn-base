//no beliefs

!start.

+!start : true  <- .print("kick starting scenario 3 - use waypoints");
						.wait(1000);
						.send(centralMember1, achieve, startUseWayPointsM25);
						.send(centralMember2, achieve, startUseWayPointsM25);
						//.send(centralMember3, achieve, startUseWayPointsM25);
						//.send(centralMember4, achieve, startUseWayPointsM25);
						.print("done starting things").
